/**
 * Ok so what the hotdog is this
 *
 * I think that my psychical health is completely ruined right now
 *
 * 18 js files in 3 days is kinda extreme
 *
 */
import Vue from 'vue'
import App from './App.vue'

import './assets/css/app.css'

Vue.config.productionTip = false

new Vue({
  render: h => h(App),
}).$mount('#app')
